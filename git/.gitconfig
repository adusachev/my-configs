[user]
	name = adusachev
	email = adusachev@yandex.ru
	#name = Usachev, Aleksey
	#email = usachev.ad@stratasolutions.ru
[alias]
	co = checkout
	st = status
	br = branch
	hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short


[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true    # use n and N to move between diff sections
    light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)

[merge]
    conflictstyle = diff3

[diff]
       colorMoved = default

[delta]
    features = line-numbers side-by-side  # custom features
